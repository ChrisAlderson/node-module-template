{
  "name": "repo-name",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "debug": "^3.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "eslint": "^4.16.0",
    "eslint-config-vixo": "^2.0.2",
    "mocha": "^5.0.0",
    "nyc": "^11.4.1",
    "standard-version": "^4.3.0"
  },
  "license": "MIT",
  "scripts": {
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "lint": "eslint {examples,test}/** *.js",
    "release": "standard-version",
    "test": "nyc -r=text -r=lcov mocha --recursive"
  },
  "engines": {
    "node": ">=6.3.0"
  },
  "keywords": [
    ""
  ],
  "files": [
    "index.js"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/author-name/repo-name.git"
  }
}
